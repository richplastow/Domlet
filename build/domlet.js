// Generated by CoffeeScript 1.9.2
(function(ªG) {
/*! Domlet 0.0.1 //// MIT Licence //// http://domlet.richplastow.com/ */
var Domlet, SomeClass, xx, ª, ªA, ªB, ªC, ªE, ªF, ªN, ªO, ªR, ªS, ªU, ªV, ªX, ªex, ªhas, ªinsert, ªis, ªisU, ªredefine, ªtype, ªuid;

ªC = 'Domlet';

ªV = '0.0.1';

Domlet = (function() {
  Domlet.prototype.C = 'Domlet';

  Domlet.prototype.toString = function() {
    return "[object " + this.C + "]";
  };

  function Domlet(config) {
    if (config == null) {
      config = {};
    }
    this.x = null;
  }

  Domlet.prototype.xx = function(xx) {
    var M;
    return M = "/domlet/src/Domlet.litcoffee Domlet.xx()\n  ";
  };

  return Domlet;

})();

xx = function(xx) {
  var M;
  return M = "/domlet/src/Domlet.litcoffee xx()\n  ";
};

ªA = 'array';

ªB = 'boolean';

ªE = 'error';

ªF = 'function';

ªN = 'number';

ªO = 'object';

ªR = 'regexp';

ªS = 'string';

ªU = 'undefined';

ªX = 'null';

ª = console.log.bind(console);

ªex = function(x, a, b) {
  var pos;
  if (-1 === (pos = a.indexOf(x))) {
    return x;
  } else {
    return b.charAt(pos);
  }
};

ªis = function(c, t, f) {
  if (t == null) {
    t = true;
  }
  if (f == null) {
    f = false;
  }
  if (c) {
    return t;
  } else {
    return f;
  }
};

ªhas = function(h, n, t, f) {
  if (t == null) {
    t = true;
  }
  if (f == null) {
    f = false;
  }
  if (-1 !== h.indexOf(n)) {
    return t;
  } else {
    return f;
  }
};

ªtype = function(x) {
  return {}.toString.call(x).match(/\s([a-z0-9]+)/i)[1].toLowerCase();
};

ªisU = function(x) {
  return ªU === typeof x;
};

ªuid = function(p) {
  return p + '_' + (Math.random() + '1111111111111111').slice(2, 18);
};

ªinsert = function(basis, overlay, offset) {
  return basis.slice(0, offset) + overlay + basis.slice(offset + overlay.length);
};

ªredefine = function(obj, name, value, kind) {
  switch (kind) {
    case 'constant':
      return Object.defineProperty(obj, name, {
        value: value,
        enumerable: true
      });
    case 'private':
      return Object.defineProperty(obj, name, {
        value: value,
        enumerable: false
      });
  }
};

if (ªF === typeof define && define.amd) {
  define(function() {
    return Domlet;
  });
} else if (ªO === typeof module && module && module.exports) {
  module.exports = Domlet;
} else {
  ªG.Domlet = Domlet;
}

SomeClass = (function() {
  SomeClass.prototype.C = 'SomeClass';

  SomeClass.prototype.toString = function() {
    return '[object SomeClass]';
  };

  function SomeClass(config) {
    var M;
    if (config == null) {
      config = {};
    }
    M = "/domlet/src/SomeClass/SomeClass.litcoffee SomeClass()\n  ";
    this.x = null;
  }

  SomeClass.prototype.xx = function(xx) {
    var M;
    return M = "/domlet/src/SomeClass/SomeClass.litcoffee SomeClass.xx()\n  ";
  };

  return SomeClass;

})();

xx = function(xx) {
  var M;
  return M = "/domlet/src/SomeClass/SomeClass.litcoffee xx()\n  ";
};
}).call(this,this);
// Example vendor file. 
